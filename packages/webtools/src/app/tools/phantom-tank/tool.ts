import type { ToolDefinition } from '@/lib/types';

const tool: ToolDefinition = {
    id: 'phantom-tank',
    name: '幻影坦克',
    description: '图片叠加 — 白底显图A，黑底显图B',
    icon: '👻',
    category: 'image',
    tags: ['图片', '叠加', '幻影', '坦克', '隐写'],
};

export default tool;
